CREATE TABLE IF NOT EXISTS ${ADDB_SCHEMA}.fields_override_config (
    "uuid" uuid PRIMARY KEY NOT NULL DEFAULT uuid_generate_v4(),
    "owner_sub" VARCHAR(64) NOT NULL,
    "label" text NOT NULL,
    "description" text,
    "object_schema_uuid" uuid REFERENCES ${ADDB_SCHEMA}.object_schema(uuid) ON DELETE CASCADE,
    "object_schema_version" int NOT NULL,
    "config" jsonb NOT NULL DEFAULT '[]'::jsonb,
    "created_at" timestamptz NOT NULL DEFAULT now(),
    "updated_at" timestamptz NOT NULL DEFAULT now()
);

DROP POLICY IF EXISTS field_override_config_policy_select ON ${ADDB_SCHEMA}.fields_override_config;
CREATE POLICY field_override_config_policy_select ON ${ADDB_SCHEMA}.fields_override_config
FOR SELECT
--  All field override configs publicly SELECTable
USING ( true );

DROP POLICY IF EXISTS field_override_config_policy_insert ON ${ADDB_SCHEMA}.fields_override_config;
CREATE POLICY field_override_config_policy_insert ON ${ADDB_SCHEMA}.fields_override_config
FOR INSERT
--  field override configs INSERTable when user has claims and is superadmin
WITH CHECK (
    (
        current_setting('request.jwt.claims', true) IS NOT NULL AND
        current_setting('request.jwt.claims', true)::json->>'sub' IS NOT NULL AND
        public.jsonb_array_to_text_array( current_setting('request.jwt.claims', true)::jsonb->'roles' ) @> array['${ADDB_SUPERADMIN_ENTITLEMENT}']
    )
);

DROP POLICY IF EXISTS field_override_config_policy_update ON ${ADDB_SCHEMA}.fields_override_config;
CREATE POLICY field_override_config_policy_update ON ${ADDB_SCHEMA}.fields_override_config
FOR UPDATE
--  field override configs UPDATEable when user has claims and is superadmin
USING (
    (
        current_setting('request.jwt.claims', true) IS NOT NULL AND
        current_setting('request.jwt.claims', true)::json->>'sub' IS NOT NULL AND
        public.jsonb_array_to_text_array( current_setting('request.jwt.claims', true)::jsonb->'roles' ) @> array['${ADDB_SUPERADMIN_ENTITLEMENT}']
    )
)
-- update policy requires both USING *and* WITH_CHECK, as the USING
-- decides which rows are updateable in the first place, and the
-- WITH_CHECK determines if the new updates fit with the policy (after
-- being approved by the USING expression)
WITH CHECK (
    (
        current_setting('request.jwt.claims', true) IS NOT NULL AND
        current_setting('request.jwt.claims', true)::json->>'sub' IS NOT NULL AND
        public.jsonb_array_to_text_array( current_setting('request.jwt.claims', true)::jsonb->'roles' ) @> array['${ADDB_SUPERADMIN_ENTITLEMENT}']
    )
);

DROP POLICY IF EXISTS field_override_config_policy_delete ON ${ADDB_SCHEMA}.fields_override_config;
CREATE POLICY field_override_config_policy_delete ON ${ADDB_SCHEMA}.fields_override_config
FOR DELETE
--  field override configs DELETEable claim sub matches owner or superuser
USING (
    (
        current_setting('request.jwt.claims', true) IS NOT NULL AND
        (
            owner_sub IS NOT NULL AND
            owner_sub = current_setting('request.jwt.claims', true)::json->>'sub'

        ) OR
        -- User is marked as superadmin
        (
            public.jsonb_array_to_text_array( current_setting('request.jwt.claims', true)::jsonb->'roles' ) @> array['${ADDB_SUPERADMIN_ENTITLEMENT}']
        )
    )
);

CREATE OR REPLACE FUNCTION ${ADDB_SCHEMA}_fields_override_config_update_trigger() RETURNS TRIGGER AS $$
BEGIN
    -- Maintain updated_at/created_at
    NEW.updated_at = now();
    IF OLD.created_at IS NOT NULL THEN
        NEW.created_at = OLD.created_at;
    END IF;
    -- Set owner_sub to the current sub if not already set, but don't override if it was already set.
    IF current_setting('request.jwt.claims', true) IS NOT NULL THEN
        IF OLD.owner_sub IS NOT NULL THEN
            -- Don't update original if it was originally set.
            NEW.owner_sub = OLD.owner_sub;
        ELSE
            --Otherwise, owner will be the current sub
            NEW.owner_sub := current_setting('request.jwt.claims', true)::json->>'sub';
        END IF;
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE OR REPLACE TRIGGER trigger_${ADDB_SCHEMA}_fields_override_config_update_trigger
BEFORE INSERT OR UPDATE ON ${ADDB_SCHEMA}.fields_override_config
FOR EACH ROW
EXECUTE FUNCTION ${ADDB_SCHEMA}_fields_override_config_update_trigger();